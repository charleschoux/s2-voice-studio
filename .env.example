# Fish Audio API Key — server-side only, never exposed to the browser.
FISH_API_KEY=

# Optional: override the Fish Audio base URL (defaults to https://api.fish.audio).
# FISH_API_BASE=https://api.fish.audio

# Optional: default model used when the client does not send a model header.
# FISH_DEFAULT_MODEL=s2.1-pro-free
